package Utils;

public class Constants {
    public static final int WAIT_TIME=1000; //since static u can call it don't need any extends or object

    public static final String PROPERTY_FILE_PATH= System.getProperty("user.dir")+ "/src/test/resources/Config/config.properties";

    public static final String SCREENSHOT_FILEPATH= System.getProperty("user.dir")+"/screenshots/";
//paths are written in uppercase
}
